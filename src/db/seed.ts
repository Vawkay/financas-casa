/**
 * Seed inicial — popula contas reais (Mercado Pago, Wise, PicPay Empresas,
 * cartão Santander, dívidas Nubank) e categorias da planilha.
 *
 * Implementação completa chega na Fase 1. Por ora é um stub seguro.
 */
async function main() {
  console.log(
    "[seed] Stub — contas e categorias iniciais serão populadas na Fase 1.",
  );
}

main().then(() => process.exit(0));
